/** @type {import('next').NextConfig} */
const nextConfig = {
  // Las Server Actions ya están activas por defecto, no hace falta la línea experimental
  images: {
    remotePatterns: [],
  },
};

export default nextConfig;